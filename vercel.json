{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ],
  "buildCommand": "pnpm --filter web build",
  "installCommand": "pnpm install --recursive --no-frozen-lockfile"
}
